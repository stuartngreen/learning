import { AlphaCharCounterPipe } from './alpha-char-counter.pipe';

describe('AlphaCharCounterPipe', () => {
  it('create an instance', () => {
    const pipe = new AlphaCharCounterPipe();
    expect(pipe).toBeTruthy();
  });
});
