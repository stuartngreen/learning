import { ExponentCalcPipe } from './exponent-calc.pipe';

describe('ExponentCalcPipe', () => {
  it('create an instance', () => {
    const pipe = new ExponentCalcPipe();
    expect(pipe).toBeTruthy();
  });
});
